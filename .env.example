# Mobile app (prefix with EXPO_PUBLIC_ to expose to client)
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
EXPO_PUBLIC_API_URL=http://localhost:3000

# API server (server-side only, never expose to client)
DATABASE_URL=postgresql://postgres:password@localhost:5432/mockket
REDIS_URL=redis://localhost:6379

# Alpaca Markets (paper trading)
ALPACA_API_KEY=your-alpaca-api-key
ALPACA_API_SECRET=your-alpaca-api-secret
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# Firebase (push notifications via FCM only — auth is handled by Supabase)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# Apple Sign In (configured in Supabase dashboard, also needs app.json entitlement)
APPLE_CLIENT_ID=com.mockket.app
